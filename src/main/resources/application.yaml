server:
  port: 8083
  error:
    include-message: always

management:
  endpoints:
    web:
      exposure:
        include: prometheus
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.example.shoppingservice: DEBUG